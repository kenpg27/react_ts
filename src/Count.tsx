import * as React from 'react';

interface ICountProps {
    int: number
}

const Count: React.FC<ICountProps> = (int) => {
    return (
        <div>

        </div>
    );
 
};

export default Count;

